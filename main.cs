//Go to Load function of Form
// then past the code below
ImageList imageList1 = new ImageList();
            cls_change_supergrid_img_size gvcs = new cls_change_supergrid_img_size();
            gvcs.InitializeGrid(this.superGridControl1, "pic", ImageSizeMode.Zoom, imageList1);
